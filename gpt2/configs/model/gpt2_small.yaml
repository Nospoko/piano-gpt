# ~25.3M parameters with 219 vocab size
n_layer: 8
n_head: 4
n_embd: 512
dropout: 0.0  # for pretraining 0 is good, for finetuning try 0.1+
bias: false  # do we use bias inside LayerNorm and Linear layers?
