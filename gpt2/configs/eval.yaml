defaults:
  - model: gpt2 # "gpt2_minimal", "gpt2_medium", "gpt2_large", "gpt2_xl"
  - data: default
  - tokenizer: exponential
  - dataset: augmented
  - tasks: all
  - _self_

dataset:
  extra_datasets: []
  augmentation:
    max_pitch_shift: 0
    speed_change_factors: []

init_from: midi-gpt2-85M-all-4096-ctx-2024-08-20-11-41.pt # should be 'midi-gpt2*'

task: multi
tasks:
  - "above_median_prediction"  # Pitch tasks

loss_masking: finetuning

system:
  device: 'cuda'  # examples: 'cpu', 'cuda', 'cuda:0', 'cuda:1' etc., or try 'mps' on macbooks
  dtype: 'float16'  # 'float32', 'bfloat16', or 'float16', the latter will auto implement a GradScaler
  compile: true  # use PyTorch 2.0 to compile the model to be faster
  data_workers: 32
